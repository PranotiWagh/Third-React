import React from 'react';
import ErrorBoundary from './Error-boundry.jsx/ErrorBoundry';

const App = () => {
    return (
        <>
            {/*<ErrorBoundary>
                <Product product='Bottle' />
            </ErrorBoundary>

            <ErrorBoundary>
                <Product product='Bags' />
    </ErrorBoundary>*/}
    {/*Portals*/}
    {/*<button onClick={setIsOpen(true)}Show Modal></button>
    <Modal setIsOpen={isopen} closeModal={closeModal}></Modal>*/}
        <Useddata></Useddata>
        </>
    );
}

export default App;
